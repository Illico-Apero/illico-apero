export default class StatusEntity {

    /**
     * 
     * @param {Number} idStatus 
     * @param {String} statusLabel 
     */
    constructor(idStatus, statusLabel) {
        this.idStatus = idStatus;
        this.statusLabel = statusLabel;
    }
}